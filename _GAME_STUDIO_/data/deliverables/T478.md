# T478 Deliverable

> [Live Token Count Testing] Check that per-agent token livestream appears on task display during execution

*Completed: 2026-09-10T22:52:56.796323*

---

DONE: Confirmed race condition bug where live token counts vanish every 0.5s when task list re-renders.
- Backend broadcasts `live_tokens` correctly during streaming (persistent_claude_cli.py:409 → broadcast.py:73)
- Frontend receives and stores tokens in `liveTokens` object, displays via `updateLiveTokenDisplay()`
- **BUG CONFIRMED**: `renderHubTasks()` rebuilds DOM with innerHTML (studio.js:1710), no re-apply of stored tokens afterward

```
[BUG] Live token counts disappear on tasks_update
[SEVERITY] major
[REPRO]
1. Start agent task that streams tokens
2. Observe live token count on task card (e.g., "5.2K")
3. Wait 0.5s for tasks_update broadcast
4. Token count vanishes from DOM
5. Reappears only on next streaming event
[EXPECTED] Live tokens persist after DOM rebuild
[FIX] Add after line 1710: Object.keys(liveTokens).forEach(agent => updateLiveTokenDisplay(agent));
```