# T739 Deliverable

> Find why cost updates fail on frontend or backend, add total session token display next to cost
Constraints: No breaking changes; Keep existing cost calculation logic

*Completed: 2026-09-17T13:39:28.552676*

---

I'll investigate the cost update issue and add total session token display.

Now I understand the codebase structure. The issue is that `renderHubMetricsBar()` displays tokens and cost, but needs to also display total session tokens. Let me check what session token data is available:

I can see that `agentStats` contains the session data including `_sessions`. Let me check the broadcast code to see what's in the agent_stats_update message:

Perfect! Now I understand. The `agentStats` object has `_sessions.boss.cumulative_tokens` and `_sessions.fleet.cumulative_tokens`. I need to:

1. Add total session tokens display to the metrics bar in `renderHubMetricsBar()`
2. Investigate why cost updates might be failing

Let me check if there's any error in how costs are being calculated or displayed:

Now let me verify the metrics bar also gets updated when agent stats are refreshed:

I need to make sure the metrics bar updates when agent stats are updated:

Now let me investigate why cost updates might be failing. Let me check how tasks get their cost data updated: