# T675 Deliverable

> [X] Find exact broadcast location; Check task creation flow end-to-end; Verify websocket connection active
[>] Investigate websocket broadcast implementation: trace why routines/tasks don't sync to fr...

*Completed: 2026-09-16T22:20:43.889932*

---

I'll investigate the websocket broadcast implementation to find why routines/tasks don't sync to frontend.

Now I need to check the handler implementations to see if they call broadcast:

Found the issue! Neither `create_task` in boss/tools.py nor `create_routine` in routine/tools.py call `broadcast_tasks_sync()` or `broadcast_schedule_update()`. Let me add those calls:

---

**T675 COMPLETED:** Added broadcast_tasks_sync() to create_task and broadcast_schedule_update() to create_routine

## Summary
Added missing websocket broadcast calls to task/routine creation handlers. Modified `studio/agents/boss/tools.py` to call `broadcast_tasks_sync()` after creating tasks, and `studio/agents/routine/tools.py` to call `broadcast_schedule_update()` after creating routines. This ensures the frontend receives immediate updates when tasks or routines are created via MCP tools.

## Friction
None