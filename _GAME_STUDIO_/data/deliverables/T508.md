# T508 Deliverable

> [BUG - MINOR] Metrics reset incorrectly in _broadcast_live_tokens

[BUG] Lines 132-135 in _broadcast_live_tokens() reset unrelated metrics mid-stream

[FILE] backends/backends/persistent_claude_cli.py...

*Completed: 2026-09-11T04:55:13.347311*

---

## Fix Applied

**Removed lines 132-135 from `_broadcast_live_tokens()`:**

```python
# REMOVED - these were incorrectly resetting metrics mid-stream:
self.last_cache_read_tokens = 0
self.last_is_error = False
self.last_error_message = None
self._tool_use_count = 0
```

**Before:** Every live token broadcast reset `_tool_use_count` to 0, causing final metrics to show incorrect tool counts.

**After:** `_broadcast_live_tokens()` only accumulates and broadcasts token counts. Metrics reset correctly in `_reset_metrics()` (lines 108-117) at the start of each new call.