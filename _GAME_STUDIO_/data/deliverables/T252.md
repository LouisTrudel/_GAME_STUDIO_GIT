# T252 Deliverable

> [WHAT] Fix QA tools API mismatch [CONTEXT] report_bug() and test_summary() have different params than documented - 'steps' and 'failed' not recognized [CONSTRAINTS] Update either the tools or the QA s...

*Completed: 2026-09-06T23:19:46.618920*

---

---

## === FIX: QA Tools API Mismatch ===

**File Updated:** `studio/skills/_tools/qa.md`

**Changes Made:**

### `report_bug`
| Docs (old) | Docs (new) - matches tools.py |
|------------|-------------------------------|
| `steps` (required) | removed |
| `expected` (required) | removed |
| `actual` (required) | removed |
| - | `assignee` (required) |
| - | `description` (optional, combines steps/expected/actual) |

### `test_summary`
| Docs (old) | Docs (new) - matches tools.py |
|------------|-------------------------------|
| `passed` (list of strings) | `passed` (boolean) |
| `failed` (list of strings) | removed |
| `bugs_filed` (number) | `bugs_reported` (list of task IDs) |
| `notes` | `summary` (required) |
| - | `task_id` (required) |

**Rationale:** Updated docs to match implementation rather than changing tools because:
1. Tools are simpler and work correctly
2. `assignee` is essential for bug routing
3. Boolean `passed` + freeform `summary` is more flexible than rigid pass/fail lists