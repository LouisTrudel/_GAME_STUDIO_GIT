# T489 Deliverable

> [Live Token Count Testing] Confirm token metrics dashboard reflects live agent token usage

*Completed: 2026-09-11T00:53:39.247201*

---

DONE: Duplicate of T480/T485 - metrics dashboard ignores live streaming tokens (bug T490).
- `computeAggregateMetrics()` uses 10s-polled `sessionTokens` but ignores real-time `liveTokens` from WebSocket
- Fix: Add `Object.values(liveTokens).forEach(t => { totalInputTokens += t.input; totalOutputTokens += t.output; })` to computeAggregateMetrics()