# T676 Deliverable

> [X] Land the broadcast fix immediately; Test routine creation syncs to frontend; Verify no server restart needed
[>] Complete T675 fix: implement the websocket broadcast for task/routine creation so f...

*Completed: 2026-09-16T22:25:36.109881*

---

I'll search for broadcast/websocket code and the routine creation endpoint to add the missing broadcast.

Now I can see the issue. The `create_routine` function tries to use `asyncio.create_task()` which won't work reliably. I need to use the sync broadcast helper like `create_task` does. Let me check if there's a sync version for schedules.

Perfect! I can see the pattern. I need to create a `broadcast_schedules_sync()` function similar to `broadcast_tasks_sync()`, and update the `create_routine()` to use it. Let me make the edits.

---

**T676 COMPLETED:** Added `broadcast_schedules_sync()` helper and fixed routine creation to broadcast immediately

## Summary
Modified `server_modules/broadcast.py` to add `broadcast_schedules_sync()` function that uses `asyncio.run_coroutine_threadsafe()` for safe sync-to-async broadcasting. Updated `studio/agents/routine/tools.py` create_routine to use the new sync helper instead of unreliable `asyncio.create_task()`, matching the pattern used in `create_task()` for tasks.

## Friction
None