# T178 Deliverable

> [WHAT] Add fetchSchedules() function to studio-core.js and call it in initApp() [CONTEXT] Schedules API endpoint exists (/api/schedules) and broadcast loop works, but frontend never fetches initial da...

*Completed: 2026-09-06T02:08:59.998810*

---

## T178 Complete

**Changes made:**

1. **studio-routines.js:4-18** - Added `fetchSchedules()` function:
   - Fetches from `/api/schedules` endpoint
   - Sets the global `routines` array with response data
   - Calls `renderRoutines()` to update UI
   - Includes console logging and error handling

2. **studio-core.js:249** - Added `fetchSchedules()` call in `initApp()`:
   - Now fetches schedules on page load alongside roles, history, suggestions, and file tree

The frontend will now load routines data on startup, fixing the issue where the Routines panel was empty until a WebSocket `schedules_update` broadcast arrived.