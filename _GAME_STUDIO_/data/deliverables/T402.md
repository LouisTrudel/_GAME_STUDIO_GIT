# T402 Deliverable

> [CONTEXT]
User approved Dynamic File Tree Injection (option #2). Research report confirmed 80% of tokens wasted on orientation. This feature injects cached file tree into agent context to reduce explo...

*Completed: 2026-09-09T13:33:32.465131*

---

DONE: Implemented dynamic file tree injection for agent context orientation.
- Created `studio/core/file_index.py` (78 lines) with `get_file_tree(project_id)` function
- Project-aware: uses external project path when active, REPO_ROOT otherwise
- 5-minute TTL cache keyed by project_id
- Skips `__pycache__`, `node_modules`, `.git`, `.pyc`, etc.
- Max depth 3, max files 50 (configurable)
- Integrated into `agent.py:_build_context()` - injects `## FILES` section before `## CONTEXT`