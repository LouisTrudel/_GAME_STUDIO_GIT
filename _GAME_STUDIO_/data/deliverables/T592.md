# T592 Deliverable

> [F] studio-tasks.js, studio/core/studio_metrics.py
[X] Use same reset logic for both tokens and cost
[>] Fix token/cost display synchronization - both values must reset together and use identical calc...

*Completed: 2026-09-11T13:29:56.880737*

---

Excellent! The fix is complete. Now:

1. When `agentStats` is available (useAgentStats = true):
   - Tokens are calculated from agentStats (lines 86-89)
   - Cost is calculated from those SAME tokens using pricing (lines 91-97)
   
2. When `agentStats` is NOT available (useAgentStats = false):
   - Both tokens AND cost come from task.cost (lines 103, 115-118)

This ensures tokens and cost are ALWAYS synchronized and use identical calculation logic. When `reset_session_tokens()` is called, both values reset together.

T592 FIXED: Unified token and cost calculation to use same data source (agentStats or task.cost)
- studio-tasks.js:80-120
- +11 lines (added useAgentStats flag and cost calculation from agentStats tokens)