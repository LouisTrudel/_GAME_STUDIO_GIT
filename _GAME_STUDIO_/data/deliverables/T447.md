# T447 Deliverable

> [CONTEXT] User expects per-agent token livestream/broadcast on tasks (showing token usage in real-time as agents work). Currently not visible in UI.

[CONSTRAINTS]
- Feature should show live token cou...

*Completed: 2026-09-10T19:23:36.912064*

---

DONE: Live token broadcast feature is **fully implemented** end-to-end.
- Backend broadcasts at `persistent_claude_cli.py:113-117`
- UI receives and displays at `studio.js:1734-1740`

---

## Summary

The live token broadcast feature is **fully implemented** across backend and frontend. The persistent Claude CLI backend streams token updates via WebSocket, and the UI displays them on in-progress task cards. If not visible, the issue is likely that other backends (Anthropic, Gemini, Ollama) don't call `broadcast_live_tokens_sync`.

## Findings

| Finding | Evidence | Action |
|---------|----------|--------|
| Backend broadcast exists | `server_modules/broadcast.py:73-88` - `broadcast_live_tokens_sync()` | Works |
| Only persistent_claude_cli calls it | `persistent_claude_cli.py:113-117` | Add to other backends |
| UI handler exists | `studio.js:1734-1740` - WebSocket handles `live_tokens` type | Works |
| UI display exists | `studio.js:1617-1636` - `updateLiveTokenDisplay()` adds `.live-token-count` span | Works |
| Works for in_progress tasks only | `studio.js:1622` - filters by `.hub-task-item.in_progress` | Correct design |

## Recommendation

**Feature is complete for persistent_claude_cli backend.** If user reports it's not working:
1. **Check which backend is running** - only `persistent_claude_cli` streams tokens
2. To support other backends, add `broadcast_live_tokens_sync()` calls to:
   - `backends/backends/anthropic.py`
   - `backends/backends/gemini.py`  
   - `backends/backends/ollama.py`

Pattern to add (from persistent_claude_cli):
```python
from server_modules.broadcast import broadcast_live_tokens_sync
broadcast_live_tokens_sync(self.agent_name, input_tokens, output_tokens)
```

## Sources

- `server_modules/broadcast.py:73-88` — broadcast function definition
- `backends/backends/persistent_claude_cli.py:110-117` — only backend that calls broadcast
- `studio.js:1617-1640` — UI display logic
- `studio.js:1734-1740` — WebSocket handler