# T233 Deliverable

> [WHAT] Update archive_old_tasks() to use to_archive_dict() [CONTEXT] Line ~834 in tasks.py currently calls to_dict() for archiving [CONSTRAINTS] Change to use new to_archive_dict() method, verify arch...

*Completed: 2026-09-06T20:50:14.037361*

---

=== T233 DELIVERABLE ===

**Changed:** `archive_old_tasks()` in `studio/core/tasks.py:909`
- Now calls `task.to_archive_dict()` instead of `task.to_dict()`
- Archived tasks use compressed schema: `{id, desc, agent, outcome, at, tokens:{in,out,usd}, friction}`

**Also updated:** `get_archive_stats()` (lines 923-948)
- Supports both old and new compressed schema
- Added `total_usd` to return stats
- Handles `tokens.in/out/usd` (new) and `total_input_tokens/total_output_tokens` (legacy)

**Schema comparison:**
| Field | to_dict() | to_archive_dict() |
|-------|-----------|-------------------|
| id | ✓ | ✓ |
| description | full | 100 chars max |
| status | enum string | outcome: ok/fail/error |
| timestamps | created_at + completed_at | single "at" field |
| cost | full nested object | tokens: {in, out, usd} |
| friction | category string | severity: low/medium/high/null |
| execution | errors + context_injected | removed |
| dependencies | array | removed |

**Expected size reduction:** ~72% per archived task