# T208 Deliverable

> [WHAT] Implement T207 recommendations: (1) Remove `result` field from Task dataclass - delete field at tasks.py:178, remove setters at lines 451-453, 469, 680, update from_dict at line 292, replace ta...

*Completed: 2026-09-06T16:37:39.278159*

---

All friction categories work correctly. 

## Deliverable Summary

Implemented T207 recommendations in `studio/core/tasks.py`:

### 1. Removed `result` field (duplicate of `output_response`)
- **Line 178**: Removed `result: Optional[str] = None` from dataclass
- **Line 450-453**: Removed `task.result = result` setter in `complete_task()`
- **Line 469**: Changed `task.result = f"FAILED: {reason}"` to `task.error = reason` in `fail_task()`
- **Line 292**: Removed `result=data.get("result")` from `from_dict()`
- **Line 680**: Changed `task.result = None` to `task.output_response = None` in `retry_task()`
- **Line 772-774**: Updated `to_context_string()` to use `output_response` instead of `result`

### 2. Added `_compute_friction_category()` method
Returns computed friction category based on execution state:
| Category | Trigger | Analysis Value |
|----------|---------|----------------|
| `error` | Task failed/errored/exec_is_error | Infrastructure issues |
| `retry` | retry_count > 0 | Flaky tasks |
| `tool_error` | exec_errors not empty | Tool reliability |
| `high_cost` | cost_usd > 1.0 | Optimization targets |
| `null` | Clean run | Baseline |

### 3. Added `friction` key to `to_dict()` output
Now includes `"friction": self._compute_friction_category()` in serialized output.

**Backwards compatibility maintained**: `from_dict()` still reads legacy fields, no breaking changes.