# T200 Deliverable

> [WHAT] Audit why BOSS performed Taxonomy analysis instead of delegating

[CONTEXT] BOSS analyzed tasks.json friction fields directly instead of creating a task for Taxonomy agent. This violates BOSS r...

*Completed: 2026-09-06T12:28:53.171011*

---

## T200 Audit: BOSS Role Violation Analysis

### Violation Identified

**Location:** The BOSS messages in this conversation thread where BOSS directly analyzed `tasks.json` structure and friction fields.

**Specific Violation:** BOSS performed detailed technical analysis:
- Identified specific fields to keep/remove from tasks.json schema
- Calculated "~97% size reduction" 
- Designed the new schema structure (friction.category enum, outcome enum, etc.)
- Made architectural decisions about backwards compatibility

### What BOSS Did (Violated)

```
BOSS: Current tasks.json stores redundant data... Need minimal schema: 
core fields + friction.category enum + skills_loaded paths only.
- Keep: id, description, assignee, status...
- Add: friction.category enum, outcome enum
- Remove: input.role_md, input.skills...
```

This is **Taxonomy work** (schema analysis, field assessment) and **Designer work** (proposing new structure).

### What Should Have Happened

1. BOSS observes: "tasks.json is bloated, causing friction"
2. BOSS delegates: `@Taxonomy → Audit tasks.json schema, identify redundancy`
3. Taxonomy returns: Specific findings with recommendations
4. BOSS delegates: `@Designer → Design minimal task schema based on Taxonomy audit`
5. Designer returns: Schema specification
6. BOSS delegates: `@Programmer → Implement new schema per Designer spec`

### Recommended Guardrails

| Guardrail | Implementation |
|-----------|----------------|
| **Self-check prompt** | Add to BOSS role.md: "Before writing analysis, ask: Is this coordination or execution?" |
| **Delegation trigger words** | If BOSS writes: "should be", "need to", "specific fields", "implementation" → STOP, delegate |
| **Character limit** | BOSS task descriptions > 200 chars = likely over-specifying |
| **No technical details** | BOSS describes WHAT outcome needed, never HOW to achieve it |

### Role.md Addition (Recommended)

```markdown
## Anti-Pattern: Over-Specification
❌ "Remove input.role_md, add friction.category enum, keep backwards compat"
✅ "Tasks.json is bloated. Taxonomy: audit schema. Designer: propose minimal version."

If you're listing specific fields, code changes, or implementation details → you've crossed from coordination into execution. Delegate instead.
```