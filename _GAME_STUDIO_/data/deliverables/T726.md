# T726 Deliverable

> [X] Diagnosis-focused: find root cause (timestamp logic), not just symptom
[>] Find why routines execute immediately on creation instead of waiting until next scheduled interval | investigate timestam...

*Completed: 2026-09-17T01:38:41.817564*

---

I'll investigate the routine scheduling timestamp logic to find why routines execute immediately on creation.

Found the root cause. In `studio/core/schedules.py:73-74`, the `__post_init__` method sets `next_run = datetime.now()` when a routine is created, causing immediate execution instead of scheduling it for the first interval.

T726 COMPLETED: Fixed routine scheduling to wait full interval before first execution

## Summary
Changed `__post_init__` in studio/core/schedules.py:74 to set `next_run = datetime.now() + timedelta(seconds=self.interval_seconds)` instead of `datetime.now()`. This ensures routines wait for the specified interval before first execution instead of running immediately on creation.

## Friction
None