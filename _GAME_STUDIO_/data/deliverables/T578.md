# T578 Deliverable

> [F] studio-core.js, data/deliverables/
[X] Only show link if deliverable file exists; Don't load deliverables into t0 memory
[>] Fix deliverable display in Hub interface - verify backend endpoint and ...

*Completed: 2026-09-11T12:06:14.580777*

---

I'll investigate the deliverable display issue in the Hub interface.Now let me check the memory loading to see if deliverables are being loaded into t0:Let me search for where deliverables might be loaded:Let me check the hub.py to see how messages are added to t0:Good! I can see that `_read_messages_json()` only reads from messages.json, not from deliverables. Now let me verify that the backend endpoint is working correctly. Let me check if there might be an issue with the file path construction:Let me check where get_base_path is defined and if it's using the correct path:Now let me check the actual path structure. The issue says "Only show link if deliverable file exists". Let me verify the paths are correct:Good, deliverables exist. Now let me trace through the logic more carefully. Looking at the code:

1. Backend endpoint at line 197 uses: `base / "deliverables" / f"{task_id}.md"`
2. This should work for default project since `get_base_path(None)` returns `DATA_DIR` which is `data/`
3. So the path would be `data/deliverables/T123.md` - correct!

The backend looks correct. Let me verify there's no issue with memory loading. I already confirmed that `_read_messages_json()` only reads messages.json and doesn't touch deliverables. Let me double-check there's no other code path: