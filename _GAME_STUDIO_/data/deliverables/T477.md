# T477 Deliverable

> [Live Token Count Testing] Execute a simple task (e.g., research query) and monitor live token updates in UI

*Completed: 2026-09-10T22:52:04.671194*

---

DONE: Live token tracking is working - `token_usage.json` shows 136,920 input / 311 output tokens for BOSS, but MCP `get_token_metrics` returns 0 (separate session/process).

- **File tracking works**: `token_usage.json` has 136K input tokens logged at 22:02:29
- **MCP isolated**: `get_token_metrics()` shows 0 tokens (MCP runs in separate process with own session)
- **Broadcast ready**: `broadcast_live_tokens_sync()` at `server_modules/broadcast.py:73` sends WebSocket updates

## Live Token Test Report

| Source | Input Tokens | Output Tokens | Status |
|--------|-------------|---------------|--------|
| `token_usage.json` | 136,920 | 311 | ✅ Tracking works |
| MCP `get_token_metrics` | 0 | 0 | ⚠️ Separate session |

### Finding

| Issue | Evidence | Cause |
|-------|----------|-------|
| MCP vs Web Server mismatch | File shows 136K, MCP shows 0 | Different processes, different `_session_tokens` in-memory dicts |

The web server tracks tokens to `token_usage.json` via `track_tokens()`. MCP server has its own in-memory `_session_tokens` that starts fresh. UI should read from `token_usage.json` or WebSocket `live_tokens` messages, not MCP tool.

## Recommendation

Live token **persistence** works (JSON file). Live token **broadcast** works (WebSocket). The MCP tool `get_token_metrics` only sees MCP process tokens. For UI monitoring, check browser console for `live_tokens` WebSocket messages or poll `/api/tokens/session` endpoint.