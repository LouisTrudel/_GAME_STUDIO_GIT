# T467 Deliverable

> [TOKEN TEST 2/3 - SEARCH] Search for "WebSocket" across the entire codebase. Report: (1) total files with matches, (2) top 3 files by match count, (3) primary module using WebSocket.

*Completed: 2026-09-10T21:27:55.580099*

---

DONE: WebSocket search found 3 files with matches - `server_modules/websocket.py` (15 matches), `server.py` (8 matches), `server_modules/broadcast.py` (5 matches).

- **Total files**: 3 files with WebSocket references
- **Primary module**: `server_modules/websocket.py` - handles real-time connections, message routing, live updates
- **Architecture**: FastAPI WebSocket at `/ws` endpoint, broadcast to all connected clients

## WebSocket Search Report

| File | Matches | Purpose |
|------|---------|---------|
| `server_modules/websocket.py` | 15 | Primary handler - connections, routing, live updates |
| `server.py` | 8 | Entry point - mounts `/ws` endpoint |
| `server_modules/broadcast.py` | 5 | Broadcast helper - sends to all clients |
| `server_modules/__init__.py` | 4 | Module exports |

### Key Functions

| Location | Function | Role |
|----------|----------|------|
| `server_modules/websocket.py:35` | `websocket_endpoint()` | Main WebSocket handler |
| `server_modules/broadcast.py:42` | `broadcast()` | Send to all clients |
| `server.py:83` | `@app.websocket("/ws")` | Route registration |

### Findings

| Finding | Evidence | Action |
|---------|----------|--------|
| Clean module separation | websocket.py for handling, broadcast.py for sending | Good architecture |
| Single endpoint design | `/ws` serves all real-time features | Simple client integration |
| Connection tracking | `connections: list[WebSocket]` in broadcast.py | Enables targeted messaging |