# T370 Deliverable

> [CONTEXT] studio_metrics.py:39-86 has thread-unsafe dict access. Lost token counts in parallel execution. [FILES] studio/studio_metrics.py:39-86 [CONSTRAINTS] Use threading.Lock(). Minimal changes. [W...

*Completed: 2026-09-09T09:00:55.895302*

---

DONE: Added threading.Lock() to protect _session_tokens dict access.
- Added `_session_lock = threading.Lock()` at module level
- Wrapped all dict mutations in `track_tokens()` with lock
- Wrapped `get_session_tokens()` reads with lock + shallow copies to prevent external mutation
- Wrapped `reset_session_tokens()` assignment with lock
- Split `_save_token_usage()` into locked wrapper + `_save_token_usage_unlocked()` for internal use