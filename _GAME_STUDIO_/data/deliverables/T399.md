# T399 Deliverable

> [CONTEXT]
Current metrics track session-wide token totals. User wants per-task granularity - each task should show its own token consumption.

[FILES]
- `studio/core/studio_metrics.py` - current sessi...

*Completed: 2026-09-09T13:15:35.580003*

---

DONE: Per-task token tracking is already implemented in backend, added UI display.
- Backend already tracks tokens per task via `track_tokens(task_id=...)` in `studio.py:720-724`
- Task schema has `cost.input_tokens`, `cost.output_tokens` fields (exposed in API)
- Session API includes `by_task` dict for historical analysis (`/api/tokens/session`)
- Added compact token display (`3.2k`) in task list UI with tooltip showing input/output breakdown
- Added CSS styling for `.hub-task-tokens` (monospace, cyan color, right-aligned)