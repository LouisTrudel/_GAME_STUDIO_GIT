# T375 Deliverable

> [Daily Codebase Review] [CONTEXT] Find real bugs and structural issues that would fail professional code review. Not metrics - actual problems.

[FILES] studio/**/*.py, server_modules/**/*.py, backend...

*Completed: 2026-09-09T09:54:11.111991*

---

DONE: Identified 5 real bugs across memory, websocket, and API layers.
- Race condition in compression, unhandled JSON exceptions, missing API error handling
- Recursive compression without depth limit, file contention during cascades

**Code Audit - 2026-09-09**

| # | File:Line | Bug | What Breaks | Fix |
|---|-----------|-----|-------------|-----|
| 1 | `studio/core/memory.py:240-247` | **Race condition** - `_is_compressing` released after recursive `append()`, allows parallel compressions during cascade | Data corruption in tier files when two agents post simultaneously | Use reentrant lock with depth counter instead of boolean flag |
| 2 | `server_modules/websocket.py:62` | **Unhandled JSONDecodeError** - `json.loads(data)` not wrapped, crashes handler on malformed JSON | Dead connection stays in `connections` list, memory leak, broadcast slowdown | Wrap `json.loads()` in try/except, continue loop on parse error |
| 3 | `backends/backends/anthropic.py:33` | **No error handling on API call** - network/rate-limit errors propagate unhandled | Task stuck `IN_PROGRESS` indefinitely, blocks dependent tasks, no failure metrics | Catch Anthropic exceptions, call `task_manager.set_error()`, add retry with backoff |
| 4 | `studio/core/history.py:266` | **Unbounded recursion** - `compress_tier()` cascades tier0→tier1→tier2... with no depth limit | Stack overflow on pathological input, DOS risk | Add `max_depth=5` parameter, stop cascade and log friction if exceeded |
| 5 | `studio/core/memory.py:266` | **File contention** - rapid nested cascade opens friction.md repeatedly during compression | Windows file lock errors, lost friction events | Batch friction writes per cascade, or pass single handle down call stack |