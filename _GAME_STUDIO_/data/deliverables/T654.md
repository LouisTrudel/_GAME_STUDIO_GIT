# T654 Deliverable

> [F] studio.js:token count display, studio.html:tasks filter UI
[X] Only affect token display logic, not data storage; Keep cumulative calculation unchanged
[>] Hide cumulative token count display when...

*Completed: 2026-09-16T16:49:04.305669*

---

I'll search for the token count display logic and tasks filter UI to understand the current implementation.

Now I can see the token display logic. The `renderHubMetricsBar()` function displays cumulative token counts. I need to modify it to hide the token/cost display when `taskFilter !== 'done'`, while keeping it visible for done tasks.

Perfect! The modification is complete. I've updated the token count display logic to only show cumulative token counts and costs when viewing DONE tasks, while hiding them for active/current tasks.

---

**T654 COMPLETED:** Conditional token display - hidden for active tasks, shown only for done tasks

## Summary
Modified `renderHubMetricsBar()` in studio.js to conditionally display token count and cost metrics based on the current task filter. Added `taskFilter === 'done'` condition to token and cost display logic (lines 491-504), ensuring cumulative metrics only appear when viewing completed tasks.

## Friction
None