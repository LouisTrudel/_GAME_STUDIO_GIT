# T671 Deliverable

> [X] Websocket broadcast on task create; Test with routine creation flow; No server restart needed
[>] Fix task creation sync: ensure new tasks/routines broadcast to frontend immediately without server...

*Completed: 2026-09-16T22:13:33.683716*

---

I'll fix the task creation sync issue by ensuring websocket broadcasts happen immediately when tasks/routines are created.

Now I need to find where `create_task` handler is implemented in boss/tools.py and add the broadcast call:

Now I can add the broadcast calls. I need to add `broadcast_tasks_sync()` after task creation in boss/tools.py and the schedule broadcast is already in schedules.py (line 229). Let me verify and add the missing broadcast:

Perfect! Now the create_task broadcasts immediately. The schedule.create already has broadcast logic (line 225-231 in schedules.py), so routines should work. Let me verify the routine creation flow also triggers task broadcasts when it spawns tasks:

The task creation now broadcasts immediately. Let me create a summary of what was done:

---

**T671 COMPLETED: Added broadcast_tasks_sync() call to task creation flow**

## Summary
Modified `studio/agents/boss/tools.py` to call `broadcast_tasks_sync()` immediately after creating a task. This ensures new tasks appear in the frontend without server restart. Schedule/routine creation already had broadcast logic in `studio/core/schedules.py:225-231` via `broadcast_schedule_update()`.

## Friction
None