# T232 Deliverable

> [WHAT] Implement to_archive_dict() method in Task class [CONTEXT] T230 spec: compressed schema with id/desc(100 char)/agent/outcome/at/tokens/friction fields - reduces archive size by ~72% [CONSTRAINT...

*Completed: 2026-09-06T20:49:38.851595*

---

Implementation complete.

---

## T232 Deliverable

Added `to_archive_dict()` method to Task class at line 264 in `studio/core/tasks.py`.

**Schema (7 fields vs 12+ in to_dict()):**
```python
{
    "id": "T001",
    "desc": "Task description (max 100 chars)...",
    "agent": "Programmer",
    "outcome": "ok",       # ok|fail|error
    "at": "2026-09-06T...",
    "tokens": {"in": 1234, "out": 567, "usd": 0.05},
    "friction": null       # null|low|medium|high
}
```

**Outcome mapping:**
- `ok` ← COMPLETED, APPROVED
- `error` ← ERROR
- `fail` ← FAILED, BLOCKED, other

**Friction mapping:**
- `null` ← clean execution
- `low` ← high_cost (>$1.00)
- `medium` ← retry, tool_error
- `high` ← error status

**Usage:** T233 will update `archive_old_tasks()` to call `to_archive_dict()` instead of `to_dict()`.