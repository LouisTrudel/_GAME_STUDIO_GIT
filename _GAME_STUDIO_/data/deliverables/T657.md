# T657 Deliverable

> [BUG - MAJOR] Task counter resets could cause ID collisions

**Given:** TaskManager creates tasks with sequential IDs (T001, T002, etc.) using `self._counter`
**When:** Server restarts or TaskManager ...

*Completed: 2026-09-16T17:07:37.932068*

---

I'll search for the task counter initialization in tasks.py to understand the current implementation and fix the ID collision risk.

Now let me read the initialization section to see how the counter is set up:

Good - the counter is loaded from disk on line 573. Now let me check the reload_from_disk method mentioned at line 578 and verify it also handles the counter properly:

Perfect! I can see the system already handles counter persistence:
- Line 573: `_load_tasks()` loads counter from disk on initialization
- Lines 622-624: `reload_from_disk()` syncs counter if disk value is higher

However, there's still a potential issue. Let me check what happens on initial `__init__`:

Excellent! The code is actually correct:
1. Line 501: Counter starts at 0
2. Line 502: `_load_tasks()` is called, which loads counter from disk (line 573)

The bug report is **incorrect** - the system already handles this properly. However, let me verify there are no edge cases by checking if there's a fallback when loading existing tasks to ensure counter is at least as high as the max existing task ID:

Let me add a safety check to ensure counter is never lower than the highest existing task ID, even if the saved counter value is corrupted:

Perfect! Now let me verify the syntax is correct by reading back the edited section:

Excellent! The code looks correct. The fix adds a safety check to prevent ID collisions even if the counter value in the JSON file gets corrupted or reset.

---

**T657 COMPLETED:** Added safety check to ensure task counter is at least as high as max existing task ID on load

## Summary
Modified `studio/core/tasks.py:575-580` to add a safety check in `_load_tasks()` that verifies the counter is never lower than the highest existing task ID. While the original counter persistence was already implemented, this prevents ID collisions if the saved counter value is corrupted or manually edited. The check scans all loaded task IDs and adjusts the counter upward if needed, logging a warning when this occurs.

## Friction
None