# T626 Deliverable

> [F] studio-core.js:1-100, index.html
[>] Debug main view layout - find and fix 50%-50% split CSS/layout causing hub/agents side-by-side

*Completed: 2026-09-11T20:29:49.204059*

---

Perfect! The issue was that `.agents-panel` had `display: flex` which overrode the `.panel { display: none; }` rule, causing the agents panel to always be visible even when inactive, creating the unwanted 50%-50% split.

---

## T626 FIXED: Removed display:flex from .agents-panel causing 50-50 split

## Summary
Fixed main view 50%-50% split bug in studio.css:1655-1658. The `.agents-panel` rule had `display: flex` which overrode `.panel { display: none; }`, causing agents panel to show alongside hub panel. Removed the `display: flex` property - `.panel.active` already provides `display: flex` when tab is active.

Files changed: studio.css

## Friction
None - straightforward CSS specificity issue.

---

## Friction Log
- [turn end] agent_note: None - straightforward CSS specificity issue.
