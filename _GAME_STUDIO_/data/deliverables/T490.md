# T490 Deliverable

> [BUG - MAJOR] Metrics dashboard ignores live streaming tokens

[BUG] Metrics dashboard ignores live streaming tokens
[SEVERITY] major
[REPRO]
1. Start an agent task that streams tokens
2. Observe live...

*Completed: 2026-09-11T00:53:05.139997*

---

DONE: Fixed metrics dashboard to include live streaming tokens in real-time totals.

- Root cause: `computeAggregateMetrics()` ignored `liveTokens` object from WebSocket stream
- Fix: Added `liveTokens` variable declaration and WebSocket handler, plus integration into metricsNow let me add `liveTokens` variable and the WebSocket handler. First, add the variable declaration:Now add the WebSocket handler. Let me find the right location:Now add the `updateBossLiveTokens` function. Let me find where UI helper functions are: