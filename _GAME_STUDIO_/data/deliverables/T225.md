# T225 Deliverable

> [WHAT] Refactor _build_system_prompt in agent.py to use explicit section markers (## SKILLS, ## ROUTER, ## ROLE, ## CONTEXT, ## TASK) in order: skills→router→context→role [CONTEXT] T224 analysis shows...

*Completed: 2026-09-06T18:50:51.428479*

---

Done. Here's the deliverable summary:

---

## T225 Deliverable: Refactored `_build_system_prompt` with Section Markers

**File:** `studio/agent.py:91-124`

**Changes:**
1. Reordered prompt sections for primacy/recency optimization: `skills → router → context → role`
2. Added explicit `## SKILLS`, `## ROUTER`, `## CONTEXT`, `## ROLE` section markers
3. Updated `_build_context` to use `## CONTEXT` and `## TASK` markers for user messages

**New Order Rationale (T224):**
- **Skills at top** → tools stay in context window even as conversation grows
- **Role at bottom** → recency effect keeps agent identity/constraints fresh

**Key code:**
```python
def _build_system_prompt(self, role_md: str, context_md: str = "") -> str:
    sections = []
    # ## SKILLS - Core tool skills + shared + agent-specific
    # ## ROUTER - Skill loading navigation  
    # ## CONTEXT - Dynamic context (injected at runtime)
    # ## ROLE - Agent identity (recency = fresh)
    return "\n\n".join(sections)
```

**Preserved:**
- All existing skill loading logic (`_get_core_tool_skills`, `load_router_skill`, shared/agent skills)
- T115 visibility methods unchanged
- No breaking changes to calling code