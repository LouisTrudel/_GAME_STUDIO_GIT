# T453 Deliverable

> [Live Token Count Testing] Check that per-agent token livestream appears on task display during execution

*Completed: 2026-09-10T21:06:17.226278*

---

DONE: Found race condition where live token counts disappear when task list re-renders every 0.5s.
- Backend broadcasts `live_tokens` correctly during streaming (persistent_claude_cli.py:409)
- Frontend stores tokens in `liveTokens` object and updates DOM via `updateLiveTokenDisplay()`
- **BUG**: `renderHubTasks()` rebuilds DOM with innerHTML, wiping live token spans; no re-apply after render

**Bug Found:**
```
[BUG] Live token counts disappear on tasks_update
[SEVERITY] major
[REPRO]
1. Start an agent task that streams tokens
2. Observe live token count appears on task card
3. Wait 0.5s for tasks_update broadcast
4. Live token count vanishes from DOM
5. Only reappears on next streaming token event
[EXPECTED] Live tokens persist after DOM rebuild
[FIX] Add after renderHubTasks() (studio.js:1710):
  Object.keys(liveTokens).forEach(agent => updateLiveTokenDisplay(agent));
```