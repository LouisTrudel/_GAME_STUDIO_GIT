# T198 Deliverable

> [WHAT] Remove hub_history.json legacy artifact and dual-write logic

[CONTEXT] hub_history.json is legacy - messages.json is now primary, session_summary moved to session_memory.md

[CONSTRAINTS]
- De...

*Completed: 2026-09-06T12:10:22.673403*

---

**T198 Complete.**

**Changes made to `studio/core/hub.py`:**
1. Removed `HISTORY_FILE` constant (line 18)
2. Removed `LEGACY_MAX_MESSAGES = 200` constant (line 22-23)
3. Simplified `_load_history()` - now only loads from `messages.json` (lines 73-82)
4. Simplified `_save_history()` - removed dual-write to `hub_history.json` (lines 84-96)
5. Updated `post()` - now trims to `MAX_MESSAGES` (50) instead of `LEGACY_MAX_MESSAGES` (200) (lines 117-119)

**File deleted:**
- `data/hub_history.json`

Session memory is now solely stored in `session_memory.md`, and message history in `messages.json`. The codebase is cleaner with ~30 lines removed.