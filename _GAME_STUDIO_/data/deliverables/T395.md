# T395 Deliverable

> [CONTEXT]
User reports: "Run Now" button in routines tab is incorrectly setting the routine to Active state. Expected behavior: clicking "Run Now" should execute the routine immediately while keeping ...

*Completed: 2026-09-09T13:01:34.654114*

---

DONE: Fixed "Run Now" button preserving paused status instead of activating routine.
- Bug was in `schedules.py:run()` which always set `status = ACTIVE` after execution
- Added `preserve_status` parameter (default `True`) to restore original status for manual triggers
- `tick()` (auto-triggers) passes `preserve_status=False` to set ACTIVE as expected
- Manual "Run Now" via WebSocket/REST now keeps routine paused if it was paused