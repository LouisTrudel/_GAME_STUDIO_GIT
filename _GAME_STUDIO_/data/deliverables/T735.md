# T735 Deliverable

> [F] agents tab, BOSS session, Fleet session
[X] Show alongside existing context size; Display cumulative tokens and estimated cost; Include both sessions side-by-side
[>] Implement total session token...

*Completed: 2026-09-17T12:39:04.308284*

---

I'll implement total session token counts and costs display in the agents tab for BOSS and Fleet sessions.

I can see that both `BossCLI` and `FleetCLI` already have `get_session_stats()` methods that return cumulative tokens. Now I need to find where the agents tab is displayed and add cost calculations alongside the existing token display.

Let me search more broadly for Python files that might contain the UI or API:

Great! Now I found the relevant files. Let me check the agent stats API and the UI:

Now I need to add session token stats to the _compute_agent_stats function. Let me check what's available from the backends:

Perfect! Now I need to:
1. Import BossCLI and FleetCLI session stats
2. Add session-level token counts and costs to the _compute_agent_stats output
3. Update the UI to display these stats

Let me first update the broadcast.py to include session stats:

Now I need to update the frontend to display this information. Let me check the agents UI file:

Perfect! I can see the UI already displays tokens. Now I need to add cost display. Let me update the renderSessionMonitor function:

Now I need to update the WebSocket handler to extract the session stats from agent stats and pass them to the display:

I see there's already a `session_stats_update` WebSocket handler! Now I need to check if the backend is sending that. Let me look at the broadcast loop: