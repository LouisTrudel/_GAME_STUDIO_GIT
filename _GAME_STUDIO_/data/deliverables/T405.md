# T405 Deliverable

> [CONTEXT] The agents tab shows token usage but values are not accurate. Per-task token tracking was implemented in T399 but display is wrong.

[FILES] `studio.js`, `studio/core/studio_metrics.py`, `se...

*Completed: 2026-09-09T16:20:38.583297*

---

DONE: Fixed token display accuracy in agents tab by adding missing `track_tokens()` call.
- Root cause: `StudioAgent.respond()` got token usage from backend but never reported to `studio_metrics.track_tokens()`
- Fix: Added import and call to `track_tokens(self.name, tokens_in, tokens_out)` in `studio/agent.py:220`
- Tokens will now accumulate correctly in `_session_tokens["by_agent"]` and display in UI