# T291 Deliverable

> [WHAT] Add shared cooldown to all suggestion buttons

[CONTEXT]
Prevents spam and misclicks by sharing a single cooldown across Accept, Reject, and Discuss buttons for each suggestion.

[REQUIREMENTS]...

*Completed: 2026-09-07T08:42:24.374654*

---

The implementation is complete. Here's a summary of the deliverable:

---

## Deliverable: Shared Cooldown for Suggestion Buttons (T291)

### Files Modified

**studio-suggestions.js:363-421**
- Replaced single-button `discussCooldowns` with shared `suggestionCooldowns` system
- All 3 buttons (Approve, Reject, Discuss) now share a 5-second cooldown
- `isOnCooldown(id)` - checks if any action is blocked
- `startSuggestionCooldown(id)` - triggers cooldown on any button click
- `startCooldownTimer(id)` - updates all buttons visually during cooldown

**studio-suggestions.js:101-117** (renderSuggestions)
- Buttons now render with shared cooldown state
- Added `cooldown` class and `disabled` attribute when on cooldown

**studio-suggestions.js:171-193** (approveSuggestion)
- Added cooldown check at function start
- Triggers shared cooldown immediately on click

**studio-suggestions.js:278-302** (rejectSuggestion)  
- Added cooldown check at function start
- Triggers shared cooldown immediately on click

**studio-suggestions.js:431-445** (discussSuggestion)
- Replaced old debounce/cooldown logic with shared cooldown
- Triggers shared cooldown immediately on click

**studio.css:1888-1899**
- Added `.suggestion-actions button.cooldown` styles
- Gray background (#444), reduced opacity (0.6), `not-allowed` cursor

### Behavior
1. User clicks any button (Approve/Reject/Discuss)
2. All 3 buttons immediately gray out and become unclickable
3. After 5 seconds, all buttons re-enable
4. If user clicks during cooldown, warning notification shows