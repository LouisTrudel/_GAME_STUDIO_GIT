# T333 Deliverable

> [WHAT] Review T332 implementation (per-project task storage) [CONTEXT] Programmer completed per-project task storage using get_base_path() pattern. [CONSTRAINTS] Verify: 1) Tasks route to projects/P00...

*Completed: 2026-09-07T19:37:50.262187*

---

Now I have enough information to complete my review. Let me verify the key findings:

## T333 QA Review: Per-Project Task Storage Implementation

### Files Reviewed
- `studio/core/tasks.py` - TaskManager with project-aware paths
- `studio/core/paths.py` - get_base_path() utility
- `studio/core/hub.py` - Integration with task_manager.set_project()
- `server_modules/routes.py` - API routes

### Verification Results

**✅ PASS: Tasks route to projects/P001/tasks.json when project active**

The implementation correctly uses `get_base_path()` pattern:
- `_get_tasks_file()` (line 449-456): Returns `base / "tasks.json"` where `base = get_base_path(self._project_name)`
- `_get_archive_file()` (line 458-465): Same pattern for archive
- `_get_deliverables_dir()` (line 467-474): Same pattern for deliverables
- `save_deliverable()` (line 91-130): Also updated to accept `project_name` parameter

**✅ PASS: Global data/tasks.json still works with no project**

In `paths.py` line 46-48:
```python
if not project_name or project_name == "default":
    return DATA_DIR  # data/
return PROJECTS_DIR / project_name  # projects/<name>/
```

The current data/tasks.json shows counter=333 with T323-T333 tasks - confirming global path works.

**✅ PASS: No regressions in task CRUD operations**

All task operations use the dynamic path methods:
- `_load_tasks()` uses `_get_tasks_file()`
- `_save_tasks()` uses `_get_tasks_file()`  
- `archive_old_tasks()` uses `_get_archive_file()`
- `_is_in_archive()` uses `_get_archive_file()`
- `get_archive_stats()` uses `_get_archive_file()`
- `complete_task()` passes `self._project_name` to `save_deliverable()`

**✅ PASS: Project switching wired correctly**

In `hub.py` line 148:
```python
task_manager.set_project(project_folder)
```

Called within `set_active_project()` alongside memory_manager and history_manager switching.

**⚠️ OBSERVATION: Minor - API routes don't accept ?project= param for tasks**

Looking at `routes.py` lines 128-169, the task endpoints (`/api/tasks`, `/api/tasks/clear`, etc.) don't call `_switch_project_context(project)` like the `/api/history` endpoint does.

However, this is likely by design: tasks should follow the hub's active project state rather than accepting per-request project params. The frontend switches projects via `/api/projects/{id}/set-active` which triggers `hub.set_active_project()` → `task_manager.set_project()`.

### Test Vector Coverage

| Category | Status | Notes |
|----------|--------|-------|
| Project active → project path | ✅ | get_base_path(P001) → projects/P001/ |
| No project → default path | ✅ | get_base_path(None) → data/ |
| Project switch saves first | ✅ | set_project() calls _save_tasks() before switching |
| Stale task recovery | ✅ | reset_in_progress_tasks() called after reload |
| Deliverables path | ✅ | Uses project_name parameter in save_deliverable() |
| Archive path | ✅ | _get_archive_file() uses get_base_path() |

### Summary

**0 bugs found.** Implementation follows the established `get_base_path()` pattern correctly. All path resolutions are dynamic via the private methods. Project switching in hub.py properly calls `task_manager.set_project()`. Backward compatibility preserved - data/tasks.json still functional when no project active.

Tool results:
[test_summary]: Test summary submitted for T333: PASSED