# T564 Deliverable

> [F] studio-tasks.js:82, studio/core/studio_metrics.py, backends/backends/persistent_claude_cli.py:377
[>] Fix token cost calculation - 553.4k tokens showing $5.90 which is incorrect pricing

*Completed: 2026-09-11T11:22:17.842076*

---

Now I see the cost comes from `task.cost?.usd`. Let me check where this is calculated in the Python backend.The cost comes from Claude CLI's `total_cost_usd` field. Let me check where it's accumulated for display in the tasks tab.The `cost_usd` comes directly from metrics. Let me check what metrics are being passed from the backend.I see the flow now. The `cost_usd` comes from Claude CLI's `total_cost_usd`. The problem is that this is the value Claude CLI reports. Let me verify by checking if there's any calculation being done or if it's just passed through. Let me also check if there's a pricing calculation anywhere else.The cost comes directly from Claude CLI. Let me check the issue more specifically - the user says 553.4k tokens showing $5.90. Let me calculate what it should be:

Using Sonnet pricing ($3/M input, $15/M output):
- If 553k were all input: 0.553 * $3 = $1.66
- If 553k were all output: 0.553 * $15 = $8.30

$5.90 is in a reasonable range for a mix of input/output. But let me check if the issue is in the frontend calculation - maybe it's calculating cost from tokens differently.I see the issue now. The tokens are aggregated from `agentStats` (line 82-88), but the `totalCost` is summed from `task.cost?.usd` (line 93). These can be from different sources/calculations.

The key question is: what is the correct cost for 553.4k tokens? Let me check the actual pricing being used. The Sonnet 4.5 pricing mentioned in studio_metrics.py is:
- Input: $3/M
- Cache read: $0.30/M  
- Cache write: $3.75/M
- Output: $15/M

For 553.4k tokens at the weighted average:
- If mostly cached input reads: 553.4k * $0.30/M = $0.17
- If input (no cache): 553.4k * $3/M = $1.66
- If output: 553.4k * $15/M = $8.30

$5.90 seems reasonable IF the tokens were mostly output. But the real question is: is the cost calculation in Claude CLI correct, or is the frontend displaying total tokens but cost from only some tasks?

Let me check if the agentStats cost_usd is being accumulated somewhere:Now I see the problem. The `_compute_agent_stats` returns token counts but NO cost_usd. The tokens are from `get_session_tokens()` in studio_metrics.py, but the cost displayed is from `task.cost.usd` which comes from individual task metrics.

The issue is: tokens displayed come from the aggregated session tokens (across all agents), but cost comes only from task-level data. These could be out of sync if some agent sessions have tokens that haven't been linked to tasks, or if tasks don't have cost data populated.

Let me verify what the actual pricing calculation should be. 553.4k tokens at $5.90 implies about $10.67 per million tokens average - which doesn't match any standard rate. Let me check if there's a custom pricing or calculation somewhere:The studio_metrics.py calculates *savings*, not actual cost. The actual cost comes from Claude CLI directly. Let me check if there's any discrepancy or if we should be calculating cost from tokens ourselves.

The real issue might be that:
1. The tokens displayed are from session_tokens (aggregated)
2. The cost displayed is from tasks (task.cost.usd), which comes from Claude CLI
3. These two might not match if there are tokens without corresponding cost tracking

Let me see if we need to calculate cost ourselves from the token breakdown: