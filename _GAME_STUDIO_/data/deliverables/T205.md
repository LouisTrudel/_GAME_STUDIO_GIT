# T205 Deliverable

> [WHAT] Research and document tool invocation enforcement patterns [CONTEXT] BOSS frequently knows to create tasks but outputs prose instead of firing create_task tool. Need strategies to enforce consi...

*Completed: 2026-09-06T12:46:34.297388*

---

**Research Complete: T205 - Tool Invocation Enforcement Patterns**

**Key findings:**

1. **Root cause**: Current architecture uses text-based `<tool>` tags with no enforcement - the model can ignore them entirely

2. **Best solution**: API-level `tool_choice: "required"/"any"` gives 99.9% compliance, but requires backend changes

3. **Immediate fixes available**:
   - Restructure BOSS prompt to eliminate the "prose path" - every example must end in tool calls
   - Add end-of-prompt enforcement block (models weight final instructions heavily)
   - Implement validation layer that detects "I should create..." without actual `<tool>` tags and retries

4. **Prioritized actions**:
   | Priority | Action | Effort |
   |----------|--------|--------|
   | 1 | Restructure BOSS prompt - remove conversational examples | Low |
   | 2 | Add "MUST USE TOOLS" block at end of system prompt | Low |
   | 3 | Add validation layer with retry nudge | Medium |
   | 4 | Migrate to native API with tool_choice | Higher |

**Report saved to:** `reports/technical/T205-tool-invocation-enforcement.md`