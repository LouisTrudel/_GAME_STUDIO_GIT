# T229 Deliverable

> [WHAT] Fix create_suggestion tool returning 'Unknown tool' [CONTEXT] Tool exists but is not being recognized when called [CONSTRAINTS] Check tool registration in suggestions.py and hub.py

*Completed: 2026-09-06T19:16:22.771776*

---

The fix is complete.

---

## Deliverable: T229 Fix

**Root Cause:** The `create_suggestion` tool was only registered in `employee_tools.py` (for Designer, Programmer, etc.) but NOT in `boss/tools.py`. When BOSS called `<tool>create_suggestion</tool>`, the handler lookup failed in `_handle_tool_calls()` at `claude_cli.py:421`, returning "Unknown tool".

**Fix Applied:**
1. **`studio/agents/boss/tools.py`** - Added `create_suggestion` tool:
   - Imported `suggestion_manager` and `VALID_CATEGORIES` from `studio.core.suggestions`
   - Added `CREATE_SUGGESTION_SCHEMA` with full parameter schema
   - Added `create_suggestion()` handler function (hardcoded `source_agent="BOSS"`)
   - Registered in `TOOLS` list and `HANDLERS` dict

2. **`studio/skills/_tools/tasks.md`** - Added tool documentation for BOSS to reference

**Integration:**
- BOSS loads `_tools/tasks.md` via `_get_core_tool_skills()` in `studio/agent.py:132-133`
- Handlers registered via `boss/tools.py:HANDLERS` dict
- Tool calls parsed by `_handle_tool_calls()` in `claude_cli.py`