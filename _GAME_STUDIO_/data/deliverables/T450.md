# T450 Deliverable

> [CONTEXT] Code identified root cause of SCH006 not displaying in UI: backend data exists but UI filtering or sync is broken.

Investigation findings:
- SCH006 exists in backend (confirmed via MCP list...

*Completed: 2026-09-10T20:16:35.641854*

---

DONE: Confirmed SCH006 exists in backend but cannot locate frontend code - need to find where UI is served from.

- SCH006 confirmed in `data/schedules.json` (lines 75-123) and backend MCP
- WebSocket broadcasts `schedules_update` via `server_modules/broadcast.py:185`
- **Blocked**: Cannot find frontend JS files - `studio-routines.js` doesn't exist in searchable paths

Let me find where the frontend is actually located.Found it! Let me check studio.html.Found "Routines" tab on line 27. Let me find the routines panel and its rendering logic.The routine list is rendered into `id="routineList"` (line 72). Now let me find the JavaScript that handles routine rendering. Let me check if there's inline JS or an external file.