# T591 Deliverable

> [F] studio/core/tasks.py:1051-1085, studio/core/tasks.py:470-550
[X] Check both active tasks and tasks_archive.json when validating dependencies
[>] Fix dependency resolution - pending tasks not trans...

*Completed: 2026-09-11T13:27:34.709561*

---

Excellent! The fix is in place. Now when tasks are archived, the system will:

1. Archive the task (remove from active tasks)
2. Call `_update_dependents(task_id)` for each archived task
3. This will check all PENDING/BLOCKED tasks that depend on the archived task
4. Since `_all_dependencies_satisfied` checks both active tasks AND the archive (line 1009), dependent tasks will correctly transition to READY

T591 FIXED: Added _update_dependents call after archiving tasks to transition pending dependents to ready
- studio/core/tasks.py:1365-1379
- +7 lines (archived_ids tracking and dependent update loop)