# T457 Deliverable

> [CONTEXT] User reports that swapping between "active" and "done" task filters in the UI does nothing. Task filter toggle is non-functional.

[CONSTRAINTS]
- Filter toggle must switch between active an...

*Completed: 2026-09-10T21:07:12.294513*

---

Code looks correct. Let me check the HTML to see if the select element has proper values.HTML has correct options: "active" and "done". Let me check if renderHubTasks gets called and trace through the full logic.The code looks correct. Let me check if there's a problem with the initial state or if the dropdown value isn't being set correctly.Let me check if `initTaskFilter` is being called on page load.Let me check what statuses tasks have vs what the filter expects.Tasks have status `APPROVED` and `IN_PROGRESS`. Let me check what status values the filter expects vs what the backend sends.The filter code at line 190 expects lowercase statuses: `['approved', 'failed', 'error', 'partial']`. But the MCP output shows `APPROVED` (uppercase). Let me check how tasks are sent from the backend and compared in JS.