# T659 Deliverable

> [F] studio.js:409-430, studio.js:473-526
[X] Don't change task filter behavior; Don't modify backend token endpoints; Keep reset button styling minimal
[>] Fix token count display randomness and ensur...

*Completed: 2026-09-16T17:33:55.852206*

---

I'll fix the token count display randomness and ensure the reset button is always visible in the taskbar.

I can see the issues:

1. **Token count randomness**: Line 491 shows token display only when viewing done/approved/completed tasks, but the condition doesn't match the actual task filter behavior
2. **Reset button visibility**: Line 523 adds the reset button inside the `items` array, which means it's always visible (this is correct)

Let me search for where `sessionTokens` is used to understand the randomness issue:

Now let me check the computeAggregateMetrics function to see where the randomness comes from:

I found the issue! The problem is on line 491 where the token display is conditionally shown based on `taskFilter`, but `sessionTokens` includes all session tokens regardless of filter. The randomness occurs because:

1. Session tokens are always fetched (line 412)
2. They're always added to the total (lines 434-435)
3. But they're only displayed when viewing done/approved/completed tasks (line 491)

The fix is to **always show tokens when they exist**, not just for done tasks. The reset button is already always visible (line 523), so that's correct.

Perfect! The fix is complete.

---

**T659 COMPLETED:** Removed task filter condition from token/cost display to fix randomness; reset button already always visible

## Summary
Modified `renderHubMetricsBar()` in studio.js:490-504 to always display token counts and cost when available, rather than only when viewing done/approved/completed tasks. This fixes the randomness caused by session tokens being globally tracked but conditionally displayed. Reset button was already always visible (line 523).

## Friction
None