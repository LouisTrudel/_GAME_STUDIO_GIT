# T195 Deliverable

> [WHAT] Add backend value validation for Raw tasks

[CONTEXT] QA Minor Issue #2: Invalid backend values silently fall through

[CONSTRAINTS]
- Add validation in studio/studio.py Raw task handling
- Ret...

*Completed: 2026-09-06T11:50:58.243447*

---

## T195 Deliverable: Backend Value Validation

**Status: Already Implemented**

The backend validation for Raw tasks is already in place at `studio/studio.py:274-279`:

```python
elif backend == "openai":
    # Future: implement openai backend
    raise ValueError(f"Backend 'openai' not yet implemented. Available: gemini, claude_cli, claude, ollama")
else:
    valid_backends = ["gemini", "claude_cli", "claude", "ollama"]
    raise ValueError(f"Invalid backend '{backend}'. Valid options: {', '.join(valid_backends)}")
```

**What it does:**
- Line 278-279 catches any invalid backend value with an explicit `else` clause
- Returns a clear error message listing valid options: `gemini, claude_cli, claude, ollama`
- The `openai` case (lines 274-276) is explicitly handled with a "not yet implemented" message

**Error flow:**
1. `_run_raw_task()` raises `ValueError` on invalid backend
2. Exception is caught at line 289, returned as `(task.id, None, {"error": e}, {})`
3. `_process_completed_futures()` at line 442-456 calls `_classify_error()` and sets error on task
4. Error message is surfaced to user via `task_manager.set_error()`

**No changes needed** - T195 requirements are already satisfied by the existing code.