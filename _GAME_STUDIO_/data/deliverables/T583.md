# T583 Deliverable

> [F] studio/core/tasks.py:1051-1085, studio/core/tasks.py:470-550
[X] Check both active tasks and tasks_archive.json when validating dependencies
[>] Investigate why dependent tasks remain PENDING - ch...

*Completed: 2026-09-11T12:59:45.291676*

---

I'll investigate why dependent tasks remain PENDING by examining the specified code sections.Now let me check when tasks get archived and how that interacts with dependency checking:Now let me check how tasks are created with dependencies:Now let me trace through what happens when dependencies are archived. Let me check when `_update_dependents` is called and if there's a timing issue:So `_update_dependents` is only called in `complete_task`. Let me check the exact flow:

1. When a task completes at line 781, it calls `_update_dependents(task_id)`
2. `_update_dependents` checks if dependencies are satisfied using `_all_dependencies_satisfied`
3. `_all_dependencies_satisfied` checks both active tasks AND archive

Let me verify there's no race condition with archiving:Let me search across the entire project to see where archive_old_tasks is called:Now let me understand the exact timing. Let me search for where this is called from:Now I see the issue! Look at the sequence:

1. Line 724: `task_manager.complete_task(task_id, response)` - this calls `_update_dependents(task_id)` at line 781 of tasks.py
2. Line 776: `task_manager.archive_old_tasks(keep_recent=10)` - this archives the just-completed task

The problem is **timing**: `_update_dependents` is called BEFORE archiving, so it's checking the active tasks dict. But if the dependency was ALREADY archived (from a previous completion), the code should handle this correctly because `_all_dependencies_satisfied` checks both active tasks and archive.

Let me look more carefully at the logic in `_update_dependents`:I found the bug! Look at line 1066: `if completed_task_id in task.dependencies:`

**The problem**: `_update_dependents` only checks tasks that have `completed_task_id` in their dependencies. But what if:

1. Task A has dependencies [T001, T002]
2. T001 completes → `_update_dependents("T001")` is called
3. It finds Task A has T001 in dependencies, but T002 is still PENDING
4. Task A stays PENDING (correct so far)
5. T001 gets archived
6. Later, T002 completes → `_update_dependents("T002")` is called
7. It finds Task A has T002 in dependencies
8. It calls `_all_dependencies_satisfied([T001, T002])`
9. T001 is now in archive, T002 is APPROVED in active tasks
10. `_all_dependencies_satisfied` should return True (checks both active and archive)
11. Task A should become READY

But the constraint at line 1066 means this only happens if `completed_task_id in task.dependencies`. So the logic should work...

Let me check if there's an issue with the archive check itself:The archive check looks correct. Let me trace through a specific scenario. Let me check the actual data files to see what's happening:Let me look for tasks with dependencies to see if any are stuck: