# T470 Deliverable

> [FILES] studio/core/tasks.py
[WHAT] Add `cancel_task(task_id)` function to task management API

*Completed: 2026-09-10T21:35:48.612382*

---

## Result

**`cancel_task(task_id)` already exists** at `studio/core/tasks.py:1083-1089`

```python
def cancel_task(self, task_id: str) -> bool:
    """Cancel a task (remove it from the system)."""
    if task_id in self.tasks:
        del self.tasks[task_id]
        self._save_tasks()
        return True
    return False
```

**Current behavior:**
- Deletes task entirely from `self.tasks` dict
- Saves to disk
- Returns `True` on success, `False` if task not found

**Related functions:**
- `retry_task(task_id)` at line 1091 - resets FAILED/ERROR tasks to READY
- `delete_task(task_id)` at line 1107 - deletes any task (UI handles confirmation)

No changes needed - function already implemented.