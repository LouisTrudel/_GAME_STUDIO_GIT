# T235 Deliverable

> [WHAT] Reformat existing tasks_archive.json to new compressed schema [CONTEXT] T232 added to_archive_dict() with compressed format (id/desc[:100]/agent/outcome/at/tokens/friction). Current archive use...

*Completed: 2026-09-06T20:55:35.967387*

---

---

## Deliverable: T235 - Archive Migration Script

### Created: `migrate_archive.py`

**Location:** `C:\Users\lou\__MY_WORK__\_GAME_STUDIO_GIT\_GAME_STUDIO_\migrate_archive.py`

### What it does:
1. Loads `data/tasks_archive.json` (old verbose format)
2. Creates backup at `data/tasks_archive.backup.json`
3. Transforms each task to compressed schema:
   - `id` → `id`
   - `description` → `desc[:100]`
   - `assignee` → `agent`
   - `status` → `outcome` (ok|fail|error)
   - `completed_at || created_at` → `at`
   - Token fields → `tokens: {in, out, usd}`
   - Friction computed from error/retry/cost fields

### Run manually:
```bash
cd C:\Users\lou\__MY_WORK__\_GAME_STUDIO_GIT\_GAME_STUDIO_
python migrate_archive.py
```

### Expected output:
```
Loading data/tasks_archive.json...
Found N tasks
Creating backup at data/tasks_archive.backup.json...
Migrating to compressed schema...
Writing migrated archive...

Migration complete!
  Old size: ~2.6MB
  New size: ~600KB (estimated)
  Reduction: ~72%
```

### Schema mapping:
| Old Field | New Field | Transform |
|-----------|-----------|-----------|
| `id` | `id` | direct |
| `description` | `desc` | truncate to 100 chars |
| `assignee` | `agent` | direct |
| `status` | `outcome` | approved/completed→ok, error→error, else→fail |
| `completed_at/created_at` | `at` | ISO timestamp |
| `cost_input_tokens/total_input_tokens` | `tokens.in` | prefer cost_* |
| `cost_output_tokens/total_output_tokens` | `tokens.out` | prefer cost_* |
| `cost_usd` | `tokens.usd` | direct |
| (computed) | `friction` | high/medium/low/null based on errors |