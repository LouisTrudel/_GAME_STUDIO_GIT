# T510 Deliverable

> [Live Token Count Testing] Check that per-agent token livestream appears on task display during execution

*Completed: 2026-09-11T05:53:24.042492*

---

## Test Results

### Happy Path ✓
Per-agent token livestream displays on task cards:
- `studio-tasks.js:214-220` shows `live-token-stream` span for in_progress tasks
- Correctly checks `task.status === 'in_progress' && liveTokens[task.assignee]`
- Formats with `formatTokens()` and styled with CSS class

### Bug Found

```
[BUG] liveTokens not cleared on task completion in studio-core.js
[SEVERITY] minor
[REPRO]
1. Start a task that streams tokens
2. Task completes
3. liveTokens[agent] retains stale values
4. Same agent's next task may show stale count briefly
[EXPECTED] Clear liveTokens when agent no longer has in_progress task
[FILE] studio-core.js:167-177
```

studio.js has cleanup (lines 1702-1707), but studio-core.js does not.

Reported as T513.