# T708 System Health Check - Critical Friction Audit

**Date**: 2026-09-17
**Focus**: Race conditions, broadcast sync, token limits

---

## 1. Race Conditions

### Status: ✅ RESOLVED

**Given**: Multiple threads accessing broadcast state  
**When**: Sync functions called from different agents  
**Then**: Protected by `_agent_statuses_lock` (line 77)

**Evidence**:
- `server_modules/broadcast.py:77` uses `with _agent_statuses_lock:` for agent status updates
- `asyncio.run_coroutine_threadsafe()` safely bridges sync→async boundary

**Conclusion**: No race conditions detected.

---

## 2. Broadcast Sync

### Status: ✅ HEALTHY

**Architecture**:
```
Sync code → run_coroutine_threadsafe() → main_loop → broadcast_to_clients()
```

**Key Functions** (broadcast.py):
- `broadcast_thinking_sync()` (83-92)
- `broadcast_live_tokens_sync()` (95-101)
- `broadcast_error_sync()` (104+)
- `broadcast_terminal_line_sync()` (233+)

**Safety Measures**:
1. `main_loop` null checks before scheduling
2. Dead connection cleanup in `broadcast_to_clients()` (53-60)
3. Lock protection for shared state

**Conclusion**: Broadcast architecture is sound.

---

## 3. Token Limits

### Status: ✅ CONFIGURED

**Boss Backend** (`boss_cli.py:23`):
```python
SESSION_TOKEN_THRESHOLD = 150_000
```

**Fleet Backend** (`fleet_cli.py:24`):
```python
SESSION_TOKEN_THRESHOLD = 150_000
```

**Both backends**:
- Auto-clear sessions at threshold
- Delete old sessions on startup (cleanup_old_sessions)
- BOSS: dedicated UUID `boss-0001-...`
- Fleet: shared UUID `fleet-0002-...`

**Conclusion**: Token limits enforced correctly.

---

## 4. Active Friction Log

**Source**: `data/memory/friction.md`

```
# Active Friction
*Updated: 2026-09-17 00:00*

No unresolved issues.
```

**Conclusion**: Clean slate.

---

## OVERALL HEALTH: ✅ GREEN

### Summary
All three critical areas are in good health:
- **Race conditions**: Mitigated via locks + coroutine threading
- **Broadcast sync**: Robust async bridge with null checks
- **Token limits**: 150K threshold enforced on both backends

### Files Reviewed
1. `server_modules/broadcast.py` (sync bridge, locks, cleanup)
2. `backends/backends/boss_cli.py` (token threshold config)
3. `backends/backends/fleet_cli.py` (token threshold config)
4. `data/memory/friction.md` (active issues log)

### Recommendations
None. System is stable.
